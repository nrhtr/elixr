#!./parse_dump
object $root;
method init()
{
    var v = [6,2,7,5,3,5,7]
    self bar(v) string showln
}

method bar(values)
{
    var sum = 0;
    
    var i = 0;
    while (i < values len) {
        sum = sum + values at(i)
        i = i + 1
    }

    return sum
}

#!./parse_dump
object $root;

var a = :alpha;
var notyetdefined = :apple;
method init()
{
    /a literal showln

    self bar

    /a literal showln
}

method bar()
{
    /a = :beta
    /notyetdefined = :orange
    /notyetdefined showln
}
